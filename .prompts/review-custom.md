# Custom Hand Rolled Design Review

Using the file contents provided in your context provide an enumeration of custom hand rolled design and code that can be replaced and simplified. Find custom code that an exact native feature, standard-library API, existing dependency, or maintained package can replace. Consider the use case of what you are review to ensure you are not suggesting to prematurely remove needed functionality or structure. Play a game of opus magnum where you are minimizing the custom hand rolled surface while maintaining "good" code.

## Notes

- If a simplification is blocked by some other design or code, then ask if that design code itself is custom hand rolled; point being do not do a superficial analysis or rely on superficial reasoning.
- prioritize the most impactful design, flows, n code that makes up the trunk of the repo.
- if there is context u are lacking that could remove load barring assumptions then list them upfront.

## Output

An enumeration of all the custom hand rolled design, code, etc that can be replaced and what replaces it.

Do not edit files, run tools, or use external research. Use only the supplied context.
