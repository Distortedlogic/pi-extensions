- [ ] Update `/home/entropybender/pi-extensions/pi-context-preload` and `/home/entropybender/.pi/agent/git/github.com/Distortedlogic/pi-context-preload` so the extension reads only `<cwd>/AGENTS.yml`, validates only its `preload` object with the existing TypeBox schema, ignores unrelated top-level keys, applies the project-trust gate before reading project configuration, excludes `AGENTS.yml` from preload content and `TREE.txt`, removes every `CONTEXT_PRELOAD.yml` fallback branch, and updates the existing unit and end-to-end tests to cover valid, absent, invalid, and untrusted `preload` configurations.

- [ ] Update `/home/entropybender/pi-extensions/pi-modes` and `/home/entropybender/.pi/agent/git/github.com/Distortedlogic/pi-modes` so package discovery reads `modes` maps from package-root `AGENTS.yml` files, trusted project modes come from `<cwd>/AGENTS.yml` after package modes, non-empty string names and string values retain the current validation, later sources retain last-value-wins behavior, and all `.pi/AGENT_MODES.yml` fallback code and documentation are removed.

- [ ] Update `/home/entropybender/pi-extensions/pi-prompts` and `/home/entropybender/.pi/agent/git/github.com/Distortedlogic/pi-prompts` so `AGENTS.yml.prompts` contains named prompt definitions and the reserved `chains` map, package and trusted project `AGENTS.yml` sources feed the existing generated-Markdown cache pipeline, prompt ordering and duplicate-name validation remain deterministic, and the path-list implementation plus every `prompts.yml` source path and fallback are removed; update the existing parser, chain, cache, unit, fixture, and end-to-end tests for the nested format.

- [ ] Merge package configuration in `pi-modes`, `pi-prompts`, and `pi-tasks` into each package-root `AGENTS.yml`: place former `CONTEXT_PRELOAD.yml` data under `preload`, former `AGENT_MODES.yml` data under `modes`, and former `prompts.yml` definitions and chains under `prompts`; delete the three legacy files, remove their package-manifest references, include `AGENTS.yml` in published files, and update existing package-archive tests to require the combined file.

- [ ] For every other maintained repository under `~/pi-extensions`, `~/repos`, and `~/.pi/agent/git/github.com/Distortedlogic`, merge root `CONTEXT_PRELOAD.yml` data into `AGENTS.yml.preload` while preserving existing `modes` and `prompts`, delete `CONTEXT_PRELOAD.yml`, and verify that every original preset, file glob, context name, mode, prompt definition, and prompt chain remains present after the merge.

- [ ] Update the affected READMEs and authoring skills to document only the combined root `AGENTS.yml` format with `preload`, `modes`, and `prompts`; remove all compatibility, fallback, and old-filename instructions, and show edits that preserve unrelated top-level keys.

- [ ] Run each affected repository's existing type checks, lint checks, tests, end-to-end checks, and package dry-run checks; then perform an ignore-independent search that excludes only Git metadata and third-party dependency directories and requires zero maintained `CONTEXT_PRELOAD.yml`, `AGENT_MODES.yml`, `prompts.yml`, manifest references, fallback branches, or fallback documentation before committing each distinct repository separately with a short migration message.
