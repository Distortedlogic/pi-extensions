- [ ] Work unit 01 — Record the pre-cutover state without changing it: capture the current branch, dirty paths, commit, origin, package name, package version, and installed Pi source for all five repositories; preserve the unrelated deleted `pi-context-preload/DIOXUS_CONTEXT_OPTIMIZATION_TASKLIST.md`, preserve the untracked `pi-tool-call-nudge/.pi/` directory, do not reset or rebase any ahead branch, and record checksums for `~/.pi/agent/settings.json`, active session JSONL files, Pi Sync state, and project `.pi/settings.json` files.
- [ ] Work unit 02 — Stop every Pi process and create timestamped backups outside active Pi directories for `~/.pi/agent/settings.json`, `~/.pi/agent/sessions/`, `~/.pi/agent/.config-sync/`, `~/.pi/config-repo`, `.pi-sync`, Pi Sync state, and affected project settings; verify that each backup is readable before any source, state, package, or repository identity changes.
- [ ] Work unit 03 — Run the current `pi-config-sync` migration flow one final time against `npm:@jachy/pi-git-sync`, approve the exact migration ID, verify the resulting configuration-sync state and baseline, disable the old package, and retain its original state only in the external backup so the renamed runtime does not contain or execute legacy migration support.
- [ ] Work unit 04 — Implement a temporary one-time structured JSONL and state migrator that parses each record, preserves headers, IDs, parent links, timestamps, line order, and user-authored prose, rewrites only extension-owned package names, custom types, generated marker prefixes, settings package sources, and state paths, writes through temporary files with atomic replacement, supports a no-write verification pass, and is excluded from every final extension package and commit.
- [ ] Work unit 05 — Define and apply the one-time durable identifier mappings: `config-sync-result` to `pi-sync/result`; all `ctree/*`, `pi-context-compress/*`, and `pi-workstream/compression` values to their `pi-compress/*` replacements; `context-preload` to `pi-preload`; `project-env-keys` to `pi-env/keys`; and `tool-call-nudge` to `pi-steering`; also rewrite extension-generated compression headers while leaving arbitrary user and assistant prose unchanged.
- [ ] Work unit 06 — Validate the migrated data set before cutover by parsing every migrated JSONL line, confirming unchanged line counts, entry IDs, parent relationships, timestamps, session headers, and valid leaves, confirming that all targeted structural identifiers use only new names, verifying Pi Sync state fingerprints and paths, and retaining the original files until the complete new runtime passes acceptance.
- [ ] Work unit 07 — Rename `pi-config-sync` to `pi-sync` in `package.json`, update the version to `0.2.0`, regenerate the root name and version in `package-lock.json`, rename `/config-sync` to `/sync`, rename the result custom type to `pi-sync/result`, rename status slots to `pi-sync` and `pi-sync-progress`, and move every runtime path from `$PI_CODING_AGENT_DIR/.config-sync/` to `$PI_CODING_AGENT_DIR/pi-sync/`.
- [ ] Work unit 08 — Remove all compatibility and migration implementation from the final `pi-sync` package: delete `src/migration.ts`, delete `test/migration.test.ts`, remove the `migrate` action and its parser, UI, completion, documentation, and tests, remove old Git-sync discovery and old state-path or symlink handling, and ensure that the runtime accepts only the new command, state root, status keys, package identity, and custom type.
- [ ] Work unit 09 — Update `pi-sync` release and user surfaces in `README.md`, `RELEASE.md`, `.github/workflows/release-gate.yml`, package metadata assertions, command tests, smoke tests, recovery tests, and two-machine tests; use only the `pi-sync` package and repository identity while preserving synchronization planning, approval, recovery, security, PUBLISH, APPLY, RECONCILE, BASELINE, THIS MACHINE, and SHARED REPOSITORY semantics.
- [ ] Work unit 10 — Rename `pi-context-compress` to `pi-compress` in `package.json`, update the version to `0.2.0`, regenerate `package-lock.json`, rename the extension factory from `piContextCompress` to `piCompress`, replace all package-branded command descriptions, notifications, panel titles, export metadata, generated headers, documentation, and direct import examples, and keep the established `/compress`, `/branch`, `/merge`, `/undo`, `/crop`, `/panel`, and `/decisions` commands.
- [ ] Work unit 11 — Replace the complete compression protocol and persistence namespace with only canonical `pi-compress/*` values: use `pi-compress/v1/range/request` and `pi-compress/v1/range/result`; rename fork, close, decision, crop, crop-tail, range, range-tail, compression, queued-task, and summary identifiers; rename `ctree` status and widget slots; rename the decision export file; remove every legacy constant, dual listener, fallback reader, and old-channel emission.
- [ ] Work unit 12 — Rename package-branded compression symbols and schemas from `Ctree*` and `CTREE_*` to clear `Compression*` and `PI_COMPRESS_*` names throughout `src/protocol.ts`, branch state, crop state, range compression, renderers, rewrite logic, panel logic, public exports, and tests; retain semantic context types such as `ContextPanel` where the name describes data rather than the old package identity.
- [ ] Work unit 13 — Update `pi-compress` tests and protocol documentation so they assert only the new package name, new custom types, new request and result channels, new generated text, new status and widget keys, new export file, unchanged public export paths, and unchanged command behavior; delete every compatibility fixture and every assertion that permits an old identifier.
- [ ] Work unit 14 — Update all compression consumers, including `pi-tasks`, `pi-workstream`, and every repository-local match, to depend on and import `pi-compress`, consume only exported canonical protocol constants, emit only new channels, write only new custom types, regenerate their lockfiles, update their tests and documentation, and remove copied or hard-coded old compression identifiers.
- [ ] Work unit 15 — Rename `pi-context-preload` to `pi-preload` in `package.json`, update the version to `0.3.0`, regenerate `package-lock.json`, change the hidden context custom type to `pi-preload`, change the temporary directory prefix to `pi-preload-tree-`, update all package titles, repository URLs, notices, tests, and documentation, and retain the stable `preload` configuration key, `PRELOAD.md`, `TREE.txt`, presets, and package-owned context source behavior.
- [ ] Work unit 16 — Rename `skills/context-preload-authoring/` to `skills/pi-preload-authoring/`, change the skill frontmatter name and every invocation to `pi-preload-authoring`, rename `template/.pi/schemas/context-preload.schema.json` to `template/.pi/schemas/pi-preload.schema.json`, update all schema references and titles, update `template/README.md.jinja` to install `pi-preload`, and provide no alias skill or old schema path.
- [ ] Work unit 17 — Update the existing `pi-preload` unit and end-to-end tests to accept only the new package identity and custom type, verify one startup injection, trusted-project isolation, preset resolution, dynamic Dioxus context, file and byte limits, tree generation, skill discovery, and the renamed temporary prefix, while leaving the unrelated deleted Dioxus task-list file unstaged.
- [ ] Work unit 18 — Rename `pi-project-env` to `pi-env` in `package.json`, update the version to `0.2.0`, regenerate `package-lock.json`, change the hidden key-list custom type to `pi-env/keys`, update repository URLs, installation and removal commands, package titles, tests, and documentation, and preserve `.env`, settings precedence, trust boundaries, process-value precedence, shutdown restoration, name-only disclosure, and value secrecy.
- [ ] Work unit 19 — Update the existing `pi-env` unit and end-to-end tests so they accept only `pi-env/keys`, verify global and project precedence, trusted and untrusted project behavior, missing-file handling, parse failures, shutdown cleanup, session replacement, no duplicate key notice, no environment-value disclosure, and a clean process restart after package replacement.
- [ ] Work unit 20 — Rename `pi-tool-call-nudge` to `pi-steering` in `package.json`, update the version to `0.2.0`, change the hidden message custom type to `pi-steering`, keep the ten-tool-call interval, user-message reset, exact steering message, and `deliverAs: "steer"`, remove every old nudge identifier, write the currently empty README with remote install and removal instructions and exact behavior, do not add a test suite or lockfile, and do not commit `.pi/`.
- [ ] Work unit 21 — Update the parent workspace identity surfaces: rename all five sections in `EXTENSIONS_INVENTORY.md`, describe only the new owners and package names, update template and package references, update any prompt, skill, mode, task, or documentation reference to the renamed packages, and retain stable feature command names only where the hard-cutover plan explicitly keeps them.
- [ ] Work unit 22 — Run repository-local exact-name audits across tracked source, tests, documentation, workflows, manifests, and lockfiles; remove every occurrence of `pi-config-sync`, `pi-context-compress`, `pi-context-preload`, `pi-project-env`, `pi-tool-call-nudge`, `py-config-sync`, `config-sync-result`, `ctree/`, `project-env-keys`, and `tool-call-nudge` from final active repository content, with no compatibility, migration, alias, fallback, or legacy exception in final code.
- [ ] Work unit 23 — Run each existing repository validation suite before commits: execute `pi-sync` check, unit, end-to-end, and release checks; execute `pi-compress` typecheck, lint, tests, and Knip; execute `pi-preload` check; execute `pi-env` check; execute `pi-steering` typecheck and lint; and run the existing checks for every updated consumer repository without adding a test suite where none exists.
- [ ] Work unit 24 — Complete clean installation validation for each renamed package by using a clean dependency install, a clean production-only install, and a provider-credential-free production extension-load check; verify command registration, skill discovery, resource loading, hidden custom types, event channels, and package exports against only the new identities.
- [ ] Work unit 25 — Create focused commits without unrelated changes: use `Rename package to pi-sync`, `Rename package to pi-compress`, `Rename package to pi-preload`, `Rename package to pi-env`, `Rename package to pi-steering`, and `Use pi-compress` in consumer repositories; inspect staged content before each commit and exclude the existing preload deletion and steering `.pi/` directory.
- [ ] Work unit 26 — Perform the Git host cutover by creating `Distortedlogic/pi-sync`, renaming the four existing GitHub repositories to `pi-compress`, `pi-preload`, `pi-env`, and `pi-steering`, setting each local `origin` to its new SSH URL, pushing all current commits and new release tags, publishing `pi-sync@0.2.0`, deprecating the old npm package without publishing an alias, and confirming that every new remote can be installed before changing active Pi settings.
- [ ] Work unit 27 — Rename the five local repository directories to `pi-sync`, `pi-compress`, `pi-preload`, `pi-env`, and `pi-steering` after their commits and remote pushes; preserve repository metadata, uncommitted unrelated state, branches, and worktrees, and update all parent workspace paths and development references to the new directory names.
- [ ] Work unit 28 — Apply the active Pi package cutover by removing `npm:@jachy/pi-git-sync` and all four old Git sources, installing the five new `git:github.com/Distortedlogic/...` remote sources, retaining the parent `../../pi-extensions` development package unless separately removed, ensuring that no old and new package pair is declared together, and making no local-path extension installation.
- [ ] Work unit 29 — Start a new Pi process and perform runtime acceptance: verify that `pi list` shows only `pi-sync`, `pi-compress`, `pi-preload`, `pi-env`, and `pi-steering`; verify `/sync` and every compression command; verify new compression channels and custom types; verify preload and skill discovery; verify environment key loading without values; verify steering after ten completed tool calls; and confirm that representative migrated sessions open without old runtime support.
- [ ] Work unit 30 — Complete the final hard-cutover audit by proving that active settings, installed package sources, Git remotes, local directories, source, tests, documentation, manifests, lockfiles, session structural fields, state directories, commands, statuses, widgets, skills, schemas, event channels, custom types, and generated text use only new identities; remove temporary migration utilities and active old state after backup verification, run final checks, commit the parent workspace as `Update extension rename references`, and leave all repositories clean.
