- [ ] Rename `pi-config-sync` to `pi-sync` in `package.json`, preserve the current version and `/config-sync` behavior, regenerate the root package name in `package-lock.json`, replace the package and repository name in `README.md`, `RELEASE.md`, `.github/workflows/release-gate.yml`, source text, and existing tests, and change the extension-owned result type from `config-sync-result` to `pi-sync/result` without adding an alias or fallback reader.
- [ ] Rename `pi-context-compress` to `pi-compress` in `package.json`, preserve the current version and existing command names, regenerate `package-lock.json`, rename `piContextCompress` to `piCompress`, replace package-branded UI and generated text, change public import examples to `pi-compress`, change the request and result channels to `pi-compress/v1/range/request` and `pi-compress/v1/range/result`, and change the package-prefixed compression, queued-task, and summary custom types to `pi-compress/*` without dual channels or old-name readers.
- [ ] Update every direct compression consumer, including `pi-tasks` and `pi-workstream`, to depend on and import `pi-compress`, use only the new exported request and result channels and package-prefixed custom types, regenerate affected lockfiles, and update the existing consumer tests and documentation in the same repositories.
- [ ] Rename `pi-context-preload` to `pi-preload` in `package.json`, preserve the current version and the `preload` configuration format, regenerate `package-lock.json`, replace package and repository references in `README.md` and existing tests, change the hidden context type from `context-preload` to `pi-preload`, and change the temporary directory prefix from `pi-context-preload-tree-` to `pi-preload-tree-` without renaming the `context-preload-authoring` skill or context schema.
- [ ] Rename `pi-project-env` to `pi-env` in `package.json`, preserve the current version and environment precedence rules, regenerate `package-lock.json`, replace package and repository references in `README.md` and existing tests, and change the hidden key-list type from `project-env-keys` to `pi-env/keys` without changing `.env`, settings, trust, cleanup, or value-secrecy behavior.
- [ ] Rename `pi-tool-call-nudge` to `pi-steering` in `package.json`, preserve the current version and ten-tool-call steering behavior, change the hidden message type from `tool-call-nudge` to `pi-steering`, replace all package and repository references, write the currently empty README with the new remote install and removal commands, and do not add a test suite, lockfile, or the untracked `.pi/` directory.
- [ ] Update the parent workspace by renaming the five extension sections in `EXTENSIONS_INVENTORY.md`, changing `template/README.md.jinja` to install `pi-preload`, and replacing every other exact old package or repository reference in tracked workspace files while leaving stable command names, configuration keys, skills, schemas, and unrelated extensions unchanged.
- [ ] Update the existing tests for each changed runtime identifier, then run `pi-sync` check and release checks, `pi-compress` typecheck, lint, tests, and Knip, `pi-preload` check, `pi-env` check, `pi-steering` typecheck and lint, and the existing checks for each changed compression consumer; also complete clean full installs, clean production installs, and provider-credential-free production extension-load checks for all five renamed packages.
- [ ] Commit only the scoped changes in each repository, excluding the unrelated deleted preload task-list file and the untracked steering `.pi/` directory; create `Distortedlogic/pi-sync`, rename the four existing GitHub repositories to `pi-compress`, `pi-preload`, `pi-env`, and `pi-steering`, update every `origin` to its new SSH URL, push all current commits, and confirm that each new remote installs successfully before changing active Pi settings.
- [ ] Rename the five local repository directories to `pi-sync`, `pi-compress`, `pi-preload`, `pi-env`, and `pi-steering`, then update development paths that point to those repository directories without changing the parent `../../pi-extensions` package entry.
- [ ] With Pi stopped, back up the affected session and settings files, migrate only machine-owned structural values from `config-sync-result`, `pi-context-compress/compression`, `pi-context-compress/queued-task`, `pi-context-compress/summary`, `context-preload`, `project-env-keys`, and `tool-call-nudge` to their new values through parsed JSONL records with atomic replacement, preserve entry IDs and parent links, and do not add runtime compatibility code.
- [ ] Replace the active settings entries for the four old Git remotes with the corresponding new Git remotes, remove `npm:@jachy/pi-git-sync`, add `git:github.com/Distortedlogic/pi-sync`, install all five extensions from their pushed remotes, ensure that no old and new package pair is declared together, and restart Pi so `pi-env` loads into a clean process.
- [ ] Verify the completed cutover by confirming that `pi list` shows only the five new package sources, the renamed commands and extension behavior load correctly, representative migrated sessions open correctly, all repositories are clean and use new origins, and exact searches of active source, tests, documentation, manifests, lockfiles, settings, and structural session fields contain none of the five old package names or old extension-owned custom types.
