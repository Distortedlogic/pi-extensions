- [ ] WU-01 — Finish `pi-preload` conventions: move the extension entry to `src/index.ts`; refactor root `agents.ts` to export one strict `configurationSchema`, `Configuration`, and `agentsSection`; load and cache only trusted top-level `pi-preload` configuration during `session_start`; use the same schema for presets; remove duplicate schemas, top-level `preload` reads, and schema-only exports; regenerate and track the lockfile; apply the final Copier baseline; pass typecheck, Biome, Node unit and end-to-end tests, clean full and production installs, and offline production loading; then configure Forgejo as `origin`, GitHub as `github`, and push Forgejo before GitHub.
- [ ] WU-02 — Finish `pi-modes` conventions: move the extension entry to `src/index.ts`; use the root `agents.ts` strict schema and conventional exports to load and cache only top-level `pi-modes` sources during `session_start`; remove hand-written top-level mode parsing; update package configuration and existing documentation; regenerate the lockfile; apply the final no-test Copier baseline while retaining the explicitly requested empty Node test; pass typecheck, Biome, the empty test, clean full and production installs, and offline production loading; then configure Forgejo as `origin`, GitHub as `github`, and push Forgejo before GitHub.
- [ ] WU-03 — Finish `pi-prompts` conventions: refactor root `agents.ts` to export one strict schema with required `prompts`, optional `chains`, and the conventional exports; discover, validate, and cache only top-level `pi-prompts` sources during `session_start`; make `resources_discover` consume the cache; remove duplicate structural validation and top-level prompt reads while preserving duplicate-name and chain-reference checks; remove schema-only exports; migrate package configuration, fixtures, conversion code, existing README examples, and tests; regenerate the lockfile; pass typecheck, Biome, all existing Node tests, clean full and production installs, and offline production loading; then configure Forgejo as `origin`, GitHub as `github`, and push Forgejo before GitHub.
- [ ] WU-04 — Finalize shared schema generation and the Copier baseline after WU-01 through WU-03 are complete: add root `src/agents.ts`, `scripts/agents-schema.ts`, exact Git dependencies, the root lockfile, and `agents:generate` and `agents:check`; generate deterministic `schema/AGENTS.schema.json`, replace the temporary template schema with that exact output, update root and template `AGENTS.yml`, schema hooks, `pi-preload-authoring`, and the template preload sentence; apply the finalized template to `pi-preload` and `pi-prompts` and its no-test variant to `pi-modes`; pin their resulting full commits in the root package; render a fresh matching `pi-*` project; pass root, package, rendered-project, clean full-install, clean production-install, and offline-load checks; then commit and push the meta-repository.
- [ ] WU-05 — Finish `pi-sync` conventions while preserving version `0.1.0` and `/config-sync`: move registration to `src/index.ts`; make the package private while keeping its GitHub repository public; remove public npm publication configuration; migrate the existing Vitest suite to `node:test`; make `test` run unit tests before the two-machine end-to-end test and make `check` run all tests; regenerate and track the lockfile; apply the final Copier baseline and Forgejo workflow; pass all unit, security, recovery, transaction, package, two-machine, clean-install, production-install, and offline-load checks; then configure Forgejo as `origin`, GitHub as `github`, and push Forgejo before GitHub.
- [ ] WU-06 — Finish `pi-compress` conventions while preserving version `0.1.0` and command names: rename `tests/` to `test/`; migrate the existing Vitest suite to `node:test`; make `check` run all tests; resolve current Biome warnings and Knip configuration hints; regenerate and track the lockfile; apply the final Copier baseline; pass typecheck, Biome, Knip, all tests, clean full and production installs, and offline production loading; then configure Forgejo as `origin`, GitHub as `github`, and push Forgejo before GitHub.
- [ ] WU-07 — Finish `pi-tasks` conventions after its remote-pinned `pi-compress` dependency and regenerated lockfile: delete the permanent `legacy-peer-deps` `.npmrc`; correct TypeScript source and test includes; migrate the existing Vitest suite to `node:test` while preserving state, replay, file, command, tool, view, and manifest coverage; split existing tests into unit and end-to-end scripts and include both in `check`; apply the final Copier baseline and repository metadata; pass all checks, clean full and production installs, and offline production loading; then configure Forgejo as `origin`, GitHub as `github`, and push Forgejo before GitHub.
- [ ] WU-08 — Update `pi-workstream` against the final remote-pinned `pi-compress` commit: make `src/index.ts` the actual registration and implementation entry; update package exports and imports; replace the duplicated range-compression path with the direct `compressRange` API; remove obsolete compression channels, custom types, and readers; migrate preload configuration; add and track a regenerated lockfile; replace `npm install --legacy-peer-deps` with `npm ci`; migrate Vitest tests to `node:test`; replace the real-provider end-to-end test with a deterministic offline provider; include unit, integration, and end-to-end tests in `check`; correct package and README descriptions; apply the final Copier and Forgejo baseline; pass all checks, clean full and production installs, and offline production loading; then configure Forgejo as `origin`, GitHub as `github`, and push Forgejo before GitHub.
- [ ] WU-09 — Finish `pi-env` conventions while preserving version `0.1.0` and all precedence, trust, cleanup, and value-secrecy behavior: move registration to `src/index.ts`; regenerate and track the lockfile; update remaining repository metadata; apply the final Copier baseline; pass typecheck, Biome, existing Node unit and end-to-end tests, clean full and production installs, and offline production loading; then configure Forgejo as `origin`, GitHub as `github`, and push Forgejo before GitHub.
- [ ] WU-10 — Finish `pi-steering` conventions while preserving version `0.1.0`, ten-tool-call behavior, and the empty Node test command: move registration to `src/index.ts`; update remaining package paths, TypeScript scope, and repository metadata; write the authorized README with purpose and remote install and removal commands; add `.copier-answers.yml`, a Node-only `.pre-commit-config.yaml` without schema or review hooks, and `.forgejo/workflows/check.yml` that runs typecheck, Biome, the empty test command, clean production installation, and offline loading; do not add test files, a package lock, or a `.pi` directory; then configure Forgejo as `origin`, GitHub as `github`, and push Forgejo before GitHub.
- [ ] WU-11 — Finish the remaining active-state cutover: restore Forgejo connectivity; create or rename the five public Forgejo repositories; configure Forgejo as `origin` and GitHub as `github` for each renamed extension; stop Pi and back up settings and affected sessions; migrate only machine-owned structural custom types and channels in saved JSONL sessions through parsed atomic replacement while preserving IDs and parent links; restart Pi; verify namespaced preload, modes, prompts and chains, trust boundaries, representative migrated sessions, and exact absence of obsolete package names and extension-owned identifiers in structural session fields.
