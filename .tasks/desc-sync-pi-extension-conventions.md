- [ ] WU-01 — Convert `pi-context-preload` into `pi-preload`: rename the package, repository, directory, factory, hidden custom-message type, and temporary-directory prefix while preserving the `pi-context-preload` configuration key, `context-preload-authoring` skill name, and context schema identity; move the extension entry to `src/index.ts`; refactor root `agents.ts` to export `configurationSchema`, `Configuration`, and `agentsSection`; load and cache only trusted `pi.extensions.pi-context-preload` configuration during `session_start`; remove duplicate schemas, old top-level readers, and schema-only package exports; migrate existing tests to the namespaced format; apply the Copier-managed baseline; regenerate the lockfile; run Node typecheck, Biome, unit tests, end-to-end tests, clean full and production installs, and an offline production extension-load check; then commit, rename the Forgejo and GitHub repositories, configure `origin` and `github`, and push Forgejo before GitHub.
- [ ] WU-02 — Bring `pi-modes` onto the shared baseline without adding a test suite: move the entry point to `src/index.ts`; add `agents.ts` with the strict `pi-modes` configuration schema and conventional exports; replace hand-written top-level `modes` parsing with cached `pi.extensions.pi-modes` validation during `session_start`; migrate package and skill examples to namespaced configuration; add the missing TypeScript, Biome, package scripts, Copier metadata, pre-commit, and adapted no-test Forgejo check files; remove the local-install README example; update the lockfile; run typecheck, Biome, schema loading through Node, clean full and production installs, and an offline direct extension-load check; then commit and push Forgejo before GitHub.
- [ ] WU-03 — Bring `pi-prompts` onto the shared configuration and repository baseline: refactor `agents.ts` to export one strict schema with required `prompts` and optional `chains`; load and cache only `pi.extensions.pi-prompts` sources during `session_start`; make `resources_discover` consume that cache; remove duplicate hand-written structural validation while retaining duplicate-name and chain-reference checks; remove schema-only package exports; migrate package configuration, fixtures, conversion code, existing README examples, and tests to the namespaced shape; apply the Copier-managed files; regenerate the lockfile; run typecheck, Biome, all existing Node tests, clean full and production installs, and an offline production extension-load check; then commit and push Forgejo before GitHub.
- [ ] WU-04 — Replace the temporary hand-written aggregate schema in the meta-repository with generated schema composition: add root `src/agents.ts` that merges the pushed `pi-preload`, `pi-modes`, and `pi-prompts` schema sections; add `scripts/agents-schema.ts`, exact package dependencies, `agents:generate`, `agents:check`, the root lockfile, and deterministic `schema/AGENTS.schema.json`; migrate root and template `AGENTS.yml` files to the namespaced format; copy the generated schema into the Copier template; update pre-commit references and the existing preload sentence in `README.md.jinja` without adding development context; update `EXTENSIONS_INVENTORY.md` and the authoring skill for final package names and namespaced examples; render the template into a fresh `pi-*` directory and verify Copier name validation, schema validation, checks, clean full and production installs, and offline extension loading; then commit and push the meta-repository.
- [ ] WU-05 — Convert `pi-config-sync` into `pi-sync`: rename the package, repository, directory, and extension-owned result type to `pi-sync/result` while preserving version `0.1.0` and `/config-sync`; move root registration to `src/index.ts`; update source, tests, package files, preload paths, release checks, and existing documentation without compatibility aliases; make the package private and remove public npm publication configuration; migrate the existing Vitest suite to `node:test`; make `check` run all tests; apply the Copier-managed baseline and Forgejo workflow; regenerate the lockfile; run all unit, security, recovery, transaction, package, two-machine, clean-install, production-install, and offline-load checks; then commit, create or rename both remotes, and push Forgejo before GitHub.
- [ ] WU-06 — Convert `pi-context-compress` into `pi-compress` before changing any consumer: rename the package, repository, directory, and factory while preserving version `0.1.0` and command names; change range channels to `pi-compress/v1/range/request` and `pi-compress/v1/range/result`; change all compression, queued-task, and summary custom types to `pi-compress/*` without dual channels or fallback readers; update public exports, source, existing documentation, tests, and examples; rename `tests/` to `test/`; migrate Vitest tests to `node:test`; make `check` run every test; apply the Copier-managed baseline; regenerate the lockfile; run typecheck, Biome, Knip, all tests, clean full and production installs, and offline production loading; then commit, rename both remotes, and push Forgejo before GitHub so consumers can pin the final source.
- [ ] WU-07 — Convert `pi-project-env` into `pi-env`: rename the package, repository, and directory while preserving version `0.1.0` and all precedence, trust, cleanup, and secrecy behavior; move root registration to `src/index.ts`; change the hidden custom-message type from `project-env-keys` to `pi-env/keys` without an old-name reader; update package paths, tests, existing install and removal commands, and repository metadata; migrate preload configuration to `pi.extensions.pi-context-preload`; apply the Copier-managed baseline; regenerate the lockfile; run typecheck, Biome, existing Node unit and end-to-end tests, clean full and production installs, and offline production loading; then commit, rename both remotes, and push Forgejo before GitHub.
- [ ] WU-08 — Convert `pi-tool-call-nudge` into `pi-steering` under its explicit exceptions: rename the package, repository, directory, and hidden message type while preserving version `0.1.0` and the ten-tool-call steering behavior; move root registration to `src/index.ts`; update package paths, preload configuration, TypeScript scope, and repository metadata; write only the explicitly authorized user-facing README purpose plus remote install and removal commands; add an adapted typecheck, Biome, and offline-load workflow without adding a test suite, lockfile, or `.pi/` directory; do not add compatibility aliases; run typecheck, Biome, clean production installation, and direct offline extension loading; then commit, rename both remotes, and push Forgejo before GitHub.
- [ ] WU-09 — Update `pi-tasks` after `pi-compress` is pushed: replace the local `pi-context-compress` dependency, bundled dependency, imports, channels, and package-owned custom types with the final pinned `pi-compress` forms; migrate package preload, mode, prompt, and chain configuration to their `pi.extensions` sections; remove the permanent `legacy-peer-deps` `.npmrc`; correct TypeScript source and test includes; migrate the existing Vitest suite to `node:test` while preserving state, replay, file, command, tool, view, and manifest coverage; split existing tests into unit and end-to-end scripts and make `check` run both; apply the Copier-managed baseline and repository metadata; regenerate the lockfile; run all checks, clean full and production installs, and offline production loading; then commit and push Forgejo before GitHub.
- [ ] WU-10 — Update `pi-workstream` after `pi-compress` is pushed: make `src/index.ts` the real registration and implementation entry without a thin wrapper; update package exports and all imports; replace duplicated compression integration with the public `pi-compress` API or protocol and remove old channels and readers; migrate preload configuration to `pi.extensions.pi-context-preload`; add a reproducible lockfile; replace `npm install --legacy-peer-deps` with `npm ci`; migrate existing Vitest tests to `node:test`; replace the real-provider end-to-end path with a deterministic offline provider or fixture; make `test` and `check` run unit, integration, and end-to-end coverage; correct existing package and README descriptions; apply the Copier-managed Forgejo baseline; run all checks, clean full and production installs, and offline production loading; then commit and push Forgejo before GitHub.
- [ ] WU-11 — Perform a cross-repository convention audit after all implementation commits: render the final Copier template and compare every populated extension against its applicable managed baseline; confirm final `src/index.ts` entry paths, private `pi-*` package metadata, namespaced `AGENTS.yml` configuration, generated-file ignores, package-owned identifier namespaces, Node test policy, no unauthorized new suites, no permanent `legacy-peer-deps`, no local `pi install` checks, and no stale package names, protocol channels, custom types, source paths, schema copies, or local file dependencies; correct each finding in its owning repository, rerun that repository’s checks and clean installation checks, and commit each repository separately.
- [ ] WU-12 — Complete the remote and local repository cutover after all checks pass: confirm each changed repository is clean and on `main`; ensure Forgejo is `origin` and GitHub is `github`; push every final commit to Forgejo first and GitHub second; verify remote installation of `pi-sync`, `pi-compress`, `pi-preload`, `pi-env`, and `pi-steering`; rename the five local directories only after those installations succeed; update development paths that name the old directories while preserving the parent meta-package path; and confirm no old and new package pair is declared together.
- [ ] WU-13 — Cut over the active Pi installation with Pi stopped: back up settings and affected sessions; migrate only the specified machine-owned structural custom types and channels through parsed JSONL records with atomic replacement while preserving entry IDs and parent links; replace old package sources in active settings with the pushed final Git sources; remove the obsolete npm sync package; install the final extensions from remotes; restart Pi so `pi-env` loads into a clean process; verify namespaced preload, modes, prompts, prompt chains, renamed commands, migrated sessions, package listings, and trust behavior; and run exact searches over tracked source, tests, manifests, lockfiles, settings, and structural session fields to prove that prohibited old package names and extension-owned identifiers are gone.
