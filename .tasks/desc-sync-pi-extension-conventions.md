- [x] WU-01 — Use only `pi-preload`, the `pi-preload` custom message type, and `.preloadignore`; remove obsolete schemas, readers, writers, aliases, and compatibility paths.
- [ ] WU-02 — Update `pi-modes` without creating a test suite: move the entry to `src/index.ts`; add root `agents.ts` with a strict non-whitespace mode-name-to-string schema and the conventional exports; load and cache only top-level `pi-modes` sources during `session_start`; remove hand-written top-level `modes` parsing; migrate package configuration and `skills/add-pi-mode/SKILL.md`; add TypeBox, TypeScript, Biome, and `typecheck`, `lint`, and `check` scripts; remove the local-install README example; update the existing lockfile; then commit and push Forgejo before GitHub.
- [ ] WU-03 — Update `pi-prompts`: refactor root `agents.ts` to export one strict schema with required `prompts`, optional `chains`, and the conventional exports; discover, validate, and cache only top-level `pi-prompts` sources during `session_start`; make `resources_discover` consume the cache; remove duplicate structural validation and top-level `prompts` reads while preserving duplicate-name and chain-reference checks; remove schema-only exports; migrate package configuration, fixtures, conversion code, existing README examples, and tests; regenerate the lockfile; pass typecheck, Biome, all existing Node tests, clean full and production installs, and offline production loading; then commit and push Forgejo before GitHub.
- [ ] WU-04 — Finalize shared schema generation and the Copier baseline after the initial WU-01 through WU-03 commits are pushed: add root `src/agents.ts`, `scripts/agents-schema.ts`, exact Git dependencies, the root lockfile, and `agents:generate` and `agents:check`; generate deterministic `schema/AGENTS.schema.json`, replace the temporary template schema with that exact output, migrate root and template `AGENTS.yml`, and update schema hooks, the authoring skill, and the existing template preload sentence; apply the finalized template to `pi-preload` and `pi-prompts`, apply its no-test variant to `pi-modes`, rerun each package check and clean installation check, and commit and push those three template updates; pin their resulting full commits in the root package, regenerate the root schema, render a fresh matching `pi-*` project, pass root and rendered-project checks, clean full and production installs, and offline loading; then commit and push the meta-repository.
- [ ] WU-05 — Rename `pi-config-sync` to `pi-sync` while preserving version `0.1.0` and `/config-sync`: move registration to `src/index.ts`; rename the extension result type to `pi-sync/result`; update every source, test, package, workflow, release, lockfile, and existing documentation reference without aliases or fallback readers; make the package private and remove public npm publication configuration; migrate the existing Vitest suite to `node:test`; make `test` run unit tests before the two-machine end-to-end test and make `check` run all tests; apply the final Copier baseline and Forgejo workflow; pass all unit, security, recovery, transaction, package, two-machine, clean-install, production-install, and offline-load checks; then commit, create or rename both remotes, set Forgejo as `origin` and GitHub as `github`, and push Forgejo before GitHub.
- [ ] WU-06 — Rename `pi-context-compress` to `pi-compress` before editing its consumers: preserve version `0.1.0` and command names; rename the factory; change the range channels to `pi-compress/v1/range/request` and `pi-compress/v1/range/result`; change compression, queued-task, and summary custom types to `pi-compress/*`; update public imports, exports, source, tests, lockfile, and existing documentation without dual channels or old readers; rename `tests/` to `test/`; migrate Vitest tests to `node:test`; make `check` run all tests; apply the final Copier baseline; pass typecheck, Biome, Knip, all tests, clean full and production installs, and offline production loading; then commit, rename both remotes, set Forgejo as `origin` and GitHub as `github`, and push Forgejo before GitHub.
- [ ] WU-07 — Update `pi-tasks` after the final `pi-compress` commit is pushed: replace the local `pi-context-compress` dependency, bundled dependency, imports, channels, and custom types with `pi-compress` pinned to that full commit; migrate package preload, modes, prompts, and chains to their direct top-level owned sections; delete the permanent `legacy-peer-deps` `.npmrc`; correct TypeScript source and test includes; migrate the existing Vitest suite to `node:test` while preserving state, replay, file, command, tool, view, and manifest coverage; split existing tests into unit and end-to-end scripts and include both in `check`; apply the final Copier baseline and repository metadata; regenerate the lockfile; pass all checks, clean full and production installs, and offline production loading; then commit and push Forgejo before GitHub.
- [ ] WU-08 — Update `pi-workstream` after the final `pi-compress` commit is pushed: make `src/index.ts` the actual registration and implementation entry; update package exports and imports; depend on `pi-compress` at that full commit and replace the duplicated range-compression path with its direct `compressRange` API; remove obsolete compression channels, custom types, and readers; migrate preload configuration; add a lockfile; replace `npm install --legacy-peer-deps` with `npm ci`; migrate Vitest tests to `node:test`; replace the real-provider end-to-end test with a deterministic offline provider; include unit, integration, and end-to-end tests in `check`; correct existing package and README descriptions; apply the final Copier and Forgejo baseline; pass all checks, clean full and production installs, and offline production loading; then commit and push Forgejo before GitHub.
- [ ] WU-09 — Rename `pi-project-env` to `pi-env` while preserving version `0.1.0` and all precedence, trust, cleanup, and value-secrecy behavior: move registration to `src/index.ts`; rename the hidden message type to `pi-env/keys`; update source, tests, package paths, lockfile, repository metadata, preload configuration, and existing remote install and removal commands without an old-name reader; apply the final Copier baseline; pass typecheck, Biome, existing Node unit and end-to-end tests, clean full and production installs, and offline production loading; then commit, rename both remotes, set Forgejo as `origin` and GitHub as `github`, and push Forgejo before GitHub.
- [ ] WU-10 — Rename `pi-tool-call-nudge` to `pi-steering` while preserving version `0.1.0` and ten-tool-call behavior: move registration to `src/index.ts`; rename the hidden message type to `pi-steering`; update package paths, preload configuration, TypeScript scope, and repository metadata without aliases; write the explicitly authorized README with the purpose plus remote install and removal commands; add `.copier-answers.yml`, a Node-only `.pre-commit-config.yaml` without schema or review hooks, and `.forgejo/workflows/check.yml` that runs typecheck, Biome, clean production installation, and offline loading; do not add a test suite, package lock, or `.pi/` directory; then commit, rename both remotes, set Forgejo as `origin` and GitHub as `github`, and push Forgejo before GitHub.
- [ ] WU-11 — Complete the repository and active-state cutover after every extension commit is pushed and installable: update `EXTENSIONS_INVENTORY.md` and remaining tracked meta-package references to the final names, run the meta-package checks, and commit and push that update; rename the five local directories and update development paths without changing the parent meta-package source; stop Pi, back up settings and affected sessions, and migrate only the specified machine-owned structural custom types and channels through parsed JSONL with atomic replacement while preserving IDs and parent links; replace old package sources in active settings, remove the obsolete npm sync package, install all final packages from pushed remotes, and restart Pi; verify package listings, renamed behavior, namespaced preload, modes, prompts and chains, trust boundaries, representative migrated sessions, and exact absence of prohibited old package names and extension-owned identifiers in tracked files, settings, and structural session fields.
