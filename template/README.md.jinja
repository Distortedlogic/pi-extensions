# {{ project_name }}

{{ project_description }}

## Install

```bash
pi install git:github.com/Distortedlogic/{{ project_name }}
```

## Context preload

Install `pi-context-preload` before you start Pi in this repository. The top-level `pi-context-preload` section in `AGENTS.yml` extends the `pi-extension` preset, which that package provides.

```bash
pi install git:github.com/Distortedlogic/pi-context-preload
```

## Development

```bash
npm install
npm run check
```
